{
  "buildCommand": "cd ../.. && yarn build:packages && cd apps/inngest-app && yarn build",
  "installCommand": "cd ../.. && yarn install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "ignoreCommand": "bash -c 'if [[ \"$VERCEL_GIT_COMMIT_REF\" == \"main\" ]] ; then exit 1; else git diff HEAD^ HEAD --quiet apps/inngest-app/ packages/; fi'"
}
