export default async function HistoryTab() {
  // Placeholder until a run-history API is available
  return (
    <div className='text-muted-foreground w-full rounded-md border p-6 text-sm'>
      Run history will appear here. Trigger a workflow from the Workflows tab to
      see recent runs.
    </div>
  );
}
